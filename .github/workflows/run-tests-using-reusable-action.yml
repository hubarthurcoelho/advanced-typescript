name: Test App

on:
  workflow_dispatch:
  push:
    branches: 
      - main

jobs:
  test:
    uses: hubarthurcoelho/my-workflows/.github/workflows/test.yml@v2
    with:
      node-version: 16
      cache-key: advanced-typescript
      checkout-ref: ${{ github.sha }}
      repository: ${{ github.repository }}
